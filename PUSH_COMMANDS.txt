git add -A

git commit -m "Fix: Show WelcomeFlow before PricingPage for new signups

- Updated AppWithPricingCheck to render WelcomeFlow first
- Added check for has_seen_welcome_flow flag
- Imported WelcomeFlow into App.tsx
- Now properly shows: Signup → WelcomeFlow (3 screens) → PricingPage"

git push

